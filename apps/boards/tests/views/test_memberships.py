from django.test import TestCase

class BoardMembershipTests(TestCase):
    def setUp(self):
        # Setup run before every test method.
        pass

    def test_board_membership(self):
        # Test that a user can join a board and leave it.
        self.assertTrue(str(1), '1')
